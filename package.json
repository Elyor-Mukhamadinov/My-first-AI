{
  "name": "suratkash-photo-editor",
  "version": "1.0.0",
  "description": "AI Photo Editor with Netlify Functions",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --jsx=automatic --platform=browser --target=es2020"
  },
  "dependencies": {
    "@google/genai": "^1.10.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-image-crop": "^11.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
